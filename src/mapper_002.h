#ifndef MAPPER_002_H
#define MAPPER_002_H

#include <stdlib.h>
#include <stdint.h>
#include "mapper.h"

extern void open_mapper_002(struct mapper *m, size_t prog_size, size_t char_size);

#endif /* _H */
